<div class="row mid-full error">
  <div class="col-xs-12">
    <b>Oops! <%= pluralize(@trip.errors.count, 'Error') %></b>
    <ul>
      <% @trip.errors.full_messages.each do |i| %>
        <li><%= i %></li>
      <% end %>
    </ul>
  </div>
</div>
