<% content_for :title do %>
  <title>Create Pin Note</title>
<% end %>

<%= render 'trips/header' %>

<div class="container">
  <% if @pin_note.errors.any? %>
    <%= render 'validation' %>
  <% end %>
  <div class="row mid-full">
    <div class="col-xs-12">
      <h1>Add a New Note</h1>
      <hr><br>
      <%= render 'form' %>
    </div>
  </div>
</div>
