<%= form_for @pin_note do |f| %>
  <%= f.label :note %>
  <%= f.text_area :note, placeholder: 'Notes' %>
  <%= link_to 'Back', :back, class: 'button secondary' %>
  <%= f.submit 'Save', class: 'button' %>
<% end %>
