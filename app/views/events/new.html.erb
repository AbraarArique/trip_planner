<% content_for :title do %>
  <title>Add an Event | Trip Planner</title>
<% end %>

<% if @event.errors.any? %>
  <%= render 'validation' %>
<% end %>

<div class="container">
  <div class="row mid-full">
    <div class="col-xs-12">
      <h1>Add a New Event</h1>
      <hr>
      <%= render 'form' %>
    </div>
  </div>
</div>
