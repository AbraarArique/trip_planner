<div class="row mid-full error">
  <div class="col-xs-12">
    <h3>
      <b>Oops! #{pluralize(@trip_plan.errors.count, 'Error')}</b>
    </h3>
    <ul>
      <% @trip_plan.errors.full_messages.each do |i| %>
        <li><%= i %></li>
      <% end %>
    </ul>
  </div>
</div>
